# 🗺️ ระบบคำนวณระยะทาง Smart Dorm

## ✅ สถานะการพัฒนา
- ✅ สร้างไฟล์ `distance-service.js` สำเร็จ
- ✅ เพิ่ม API Endpoints สำหรับคำนวณระยะทาง  
- ✅ อัพเดต Chatbot ให้รองรับคำถามเกี่ยวกับระยะทาง
- ✅ ไม่มี compilation errors
- ⏳ รอทดสอบกับข้อมูลจริง

---

## 📁 ไฟล์ที่สร้างและแก้ไข

### 1. **distance-service.js** (NEW)
**ตำแหน่ง:** `Backend/distance-service.js`

**ฟังก์ชันหลัก:**
- `calculateRoadDistance(lat1, lon1, lat2, lon2, mode)` - คำนวณระยะทางผ่าน OpenRouteService API
- `calculateDistanceBetweenDorms(dorm1, dorm2)` - คำนวณระยะทางระหว่างหอพัก 2 หอ (เดิน + ขับรถ)
- `haversineDistance(lat1, lon1, lat2, lon2)` - คำนวณระยะทางแบบเส้นตรง (fallback)
- `formatDuration(seconds)` - แปลงวินาทีเป็น "X ชม. Y นาที"

**โหมดการเดินทาง:**
- `foot-walking` - เดินเท้า 🚶
- `driving-car` - ขับรถ 🚗  
- `cycling-regular` - ปั่นจักรยาน 🚴

**API Key:** 
```
eyJvcmciOiI1YjNjZTM1OTc4NTExMDAwMDFjZjYyNDgiLCJpZCI6IjA2MzA3MGQ1ZWVjMDRiZDJhYmEzYzk4MjExNjgxYmZjIiwiaCI6Im11cm11cjY0In0=
```

**Limits:**
- 40 requests/minute
- 2,000 requests/day

---

### 2. **index.js** (MODIFIED)
**ตำแหน่ง:** `Backend/index.js`

#### การเปลี่ยนแปลง:

**1) เพิ่ม import** (บรรทัด 25):
```javascript
const { calculateDistanceBetweenDorms, calculateRoadDistance } = require('./distance-service');
```

**2) เพิ่ม API Endpoint: `/distance-between-dorms`** (บรรทัด ~3613)
- Method: `POST`
- รับ: `{ dorm1_id, dorm2_id }`
- คืน: ระยะทางเดิน + ขับรถระหว่าง 2 หอ

**ตัวอย่างการใช้งาน:**
```bash
curl -X POST http://localhost:5001/distance-between-dorms \
  -H "Content-Type: application/json" \
  -d '{
    "dorm1_id": 1,
    "dorm2_id": 2
  }'
```

**ตัวอย่าง Response:**
```json
{
  "dorm1": { "id": 1, "name": "หอพักมหาสารคาม" },
  "dorm2": { "id": 2, "name": "หอพักธรรมศาสตร์" },
  "distance": {
    "walking": {
      "distance_km": 2.45,
      "distance_m": 2450,
      "duration_min": 30,
      "duration_text": "30 นาที",
      "mode_th": "เดินเท้า"
    },
    "driving": {
      "distance_km": 2.1,
      "distance_m": 2100,
      "duration_min": 5,
      "duration_text": "5 นาที",
      "mode_th": "ขับรถ"
    }
  }
}
```

**3) เพิ่ม API Endpoint: `/distance-to-dorm`** (บรรทัด ~3700)
- Method: `POST`
- รับ: `{ latitude, longitude, dorm_id, mode }`
- คืน: ระยะทางจากจุดที่กำหนดไปยังหอพัก

**ตัวอย่างการใช้งาน:**
```bash
curl -X POST http://localhost:5001/distance-to-dorm \
  -H "Content-Type: application/json" \
  -d '{
    "latitude": 16.246847,
    "longitude": 103.251831,
    "dorm_id": 1,
    "mode": "foot-walking"
  }'
```

**4) เพิ่มฟังก์ชัน `answerDistanceQuery()`** (บรรทัด ~3867)
- ตรวจจับคำถามเกี่ยวกับระยะทาง: "ระยะทาง", "ไกล", "ใกล้", "กี่กิโล", "ห่างกัน"
- หาชื่อหอพัก 2 หอจากข้อความ
- คำนวณระยะทางอัตโนมัติ
- ส่งคำตอบพร้อม emoji 📍🚶🚗

**5) อัพเดต Chatbot Endpoints** (2 จุด: บรรทัด ~2650 และ ~4117)
- เพิ่มการตรวจสอบ `answerDistanceQuery()` ก่อน `answerSimpleQuery()`
- รองรับคำถามแบบ: "ระยะทางระหว่างหอ A กับหอ B เท่าไหร่"

---

## 🧪 วิธีการทดสอบ

### ขั้นตอนที่ 1: ตรวจสอบ Dependencies
```powershell
cd Backend
npm list axios
```

หากยังไม่มี axios:
```powershell
npm install axios
```

### ขั้นตอนที่ 2: เริ่มเซิร์ฟเวอร์
```powershell
cd Backend
node index.js
```

### ขั้นตอนที่ 3: ทดสอบกับคำถามจริง

**ทดสอบด้วยไฟล์ทดสอบพิเศษ (แนะนำ!):**
```powershell
cd Backend
node test-specific-distance.js
```

ไฟล์นี้จะ:
- ✅ แสดงรายชื่อหอพักทั้งหมดในระบบ
- ✅ ค้นหาหอพักที่เกี่ยวข้องกับคำถาม
- ✅ ทดสอบหลายรูปแบบคำถาม
- ✅ แสดงผลลัพธ์แบบละเอียด

### ขั้นตอนที่ 4: ทดสอบ API

**ทดสอบ /distance-between-dorms:**
```powershell
Invoke-RestMethod -Uri "http://localhost:5001/distance-between-dorms" `
  -Method POST `
  -ContentType "application/json" `
  -Body '{"dorm1_id": 1, "dorm2_id": 2}'
```

**ทดสอบ /distance-to-dorm:**
```powershell
Invoke-RestMethod -Uri "http://localhost:5001/distance-to-dorm" `
  -Method POST `
  -ContentType "application/json" `
  -Body '{"latitude": 16.246, "longitude": 103.251, "dorm_id": 1, "mode": "foot-walking"}'
```

### ขั้นตอนที่ 4: ทดสอบ Chatbot

**ผ่าน Frontend Chatbot Widget:**
1. เปิดเว็บไซต์
2. คลิกปุ่มแชทบอท
3. ลองพิมพ์คำถาม:
   - "หอดิอินฟินิตโอพิวเลนส์ห่างกับหอพักสตรีศุุภลักษณ์เท่าไหร่"
   - "ระยะทางระหว่างหอพักมหาสารคามกับหอพักธรรมศาสตร์เท่าไหร์"
   - "หอพัก A ห่างจากหอพัก B กี่กิโล"
   - "หอพัก X กับหอพัก Y ไกลกันไหม"

**💡 เทคนิคการถาม:**
- ระบบรองรับการพิมพ์ที่ไม่มีช่องว่างระหว่างคำ
- ระบบจะตัดคำว่า "หอพัก" หรือ "หอ" ออกอัตโนมัติ
- สามารถใช้คำว่า "กับ", "และ", "ห่างจาก" แทนกันได้
- ไม่จำเป็นต้องพิมพ์ชื่อให้ตรงทุกตัวอักษร

**ผ่าน API โดยตรง:**
```powershell
Invoke-RestMethod -Uri "http://localhost:5001/chatbot" `
  -Method POST `
  -ContentType "application/json" `
  -Body '{"message": "ระยะทางระหว่างหอพักมหาสารคามกับหอพักธรรมศาสตร์เท่าไหร์"}'
```

**ตัวอย่างคำตอบจาก Chatbot:**
```
📍 ระยะทางระหว่าง หอพักมหาสารคาม กับ หอพักธรรมศาสตร์

🚶 เดินเท้า: 2.45 กม. (ประมาณ 30 นาที)
🚗 ขับรถ: 2.1 กม. (ประมาณ 5 นาที)

💡 ระยะทางคำนวณตามเส้นทางถนนจริงค่ะ
```

---

## ⚠️ ข้อกำหนดสำคัญ

### 1. ข้อมูลพิกัดในฐานข้อมูล
หอพักต้องมีข้อมูลใน table `location_coordinates`:
```sql
SELECT d.id, d.name, lc.latitude, lc.longitude
FROM dorms d
LEFT JOIN location_coordinates lc ON d.id = lc.dorm_id
WHERE d.status = 'approved';
```

หากหอพักไม่มีพิกัด จะได้คำตอบ:
> "ขอโทษค่ะ หอพักยังไม่มีข้อมูลตำแหน่งที่ชัดเจน กรุณาติดต่อเจ้าของหอพักเพื่ออัพเดตข้อมูลค่ะ 📍"

### 2. Format พิกัด
- OpenRouteService ใช้ `[longitude, latitude]` (กลับกัน!)
- ฐานข้อมูลเก็บ `latitude, longitude` (ปกติ)
- distance-service.js จัดการการแปลงให้อัตโนมัติ

### 3. Rate Limiting
ระบบมี fallback ไปใช้ Haversine (เส้นตรง) เมื่อ:
- API ล้ม/ช้า (timeout 10 วินาที)
- เกิน quota (40 req/min)
- Network error

---

## 🎯 คำถามที่ Chatbot รองรับ

### รูปแบบที่ใช้ได้:
- ✅ "ระยะทางระหว่าง [หอ A] กับ [หอ B] เท่าไหร์"
- ✅ "[หอ A] ห่างจาก [หอ B] กี่กิโล"
- ✅ "[หอ A] กับ [หอ B] ไกลกันไหม"
- ✅ "[หอ A] ใกล้ [หอ B] ไหม"
- ✅ "กี่เมตรระหว่าง [หอ A] กับ [หอ B]"

### คีย์เวิร์ดที่ตรวจจับ:
- ระยะทาง
- ไกล
- ใกล้
- กี่กิโล
- กี่เมตร
- ห่างกัน

---

## 🐛 การ Debug

### ตรวจสอบ Console Logs:
```
🗺️ Calculating distance via OpenRouteService...
✅ OpenRouteService API Success
🗺️ Haversine Distance (Fallback)
❌ OpenRouteService API Error
❌ Distance Calculation Error
❌ Distance Query Error
```

### ข้อผิดพลาดที่พบบ่อย:

**1. "ไม่พบข้อมูลพิกัด"**
- สาเหตุ: หอพักไม่มีข้อมูลใน `location_coordinates`
- แก้ไข: เพิ่มพิกัดผ่าน Owner Dashboard

**2. "เกิดข้อผิดพลาดในการคำนวณระยะทาง"**
- สาเหตุ: OpenRouteService API ล้มหรือ timeout
- ระบบจะ fallback ไปใช้ Haversine อัตโนมัติ

**3. "ฉันไม่พบชื่อหอพักในคำถามค่ะ"**
- สาเหตุ: ระบบไม่เจอชื่อหอพักในข้อความ
- แก้ไข: 
  1. รัน `node test-specific-distance.js` เพื่อดูชื่อหอพักที่มีในระบบ
  2. ตรวจสอบว่าชื่อหอพักถูกต้องหรือไม่
  3. ลองพิมพ์ชื่อให้ชัดเจนขึ้น เช่น "ระยะทางระหว่าง [ชื่อหอพักเต็ม] กับ [ชื่อหอพักเต็ม]"

**4. "พบหอพัก ... แต่ไม่สามารถหาหอพักที่สองได้"**
- สาเหตุ: ระบบเจอแค่หอพักเดียว
- แก้ไข: 
  1. ใช้คำว่า "กับ" หรือ "และ" หรือ "ห่างจาก" เพื่อแยกชื่อหอพัก
  2. ตรวจสอบชื่อหอพักที่สองว่าถูกต้องหรือไม่

**5. Axios not found**
- แก้ไข: `npm install axios`

**6. "Cannot find module './distance-service'"**
- สาเหตุ: ไฟล์ `distance-service.js` ไม่อยู่ใน Backend folder
- แก้ไข: ตรวจสอบว่าไฟล์อยู่ที่ `c:\รวมทั้งหมด\TRUE2\Backend\distance-service.js`

---

## 📊 ข้อมูลเทคนิค

### OpenRouteService API
- **Base URL:** https://api.openrouteservice.org/v2/directions/{mode}
- **Mode Options:** foot-walking, driving-car, cycling-regular
- **Request Format:** GET with query params
- **Response:** GeoJSON with distance (meters) and duration (seconds)

### ระยะทางที่คำนวณได้
- **Road Distance** - ระยะทางตามถนนจริง (OpenRouteService)
- **Straight Line** - ระยะทางเส้นตรง (Haversine - Fallback)
- **Accuracy:** ขึ้นอยู่กับ quality ของ OpenStreetMap data

### Performance
- **Typical Response Time:** 500-1500ms (OpenRouteService)
- **Fallback Response Time:** <10ms (Haversine)
- **Caching:** ไม่มี (อาจเพิ่มในอนาคตเพื่อ optimize)

---

## 🔮 แนวทางพัฒนาต่อ

### Phase 2 (Optional):
1. **Caching System** - เก็บ cache ระยะทางที่คำนวณแล้ว
2. **Batch Calculation** - คำนวณระยะทางหลายหอพักพร้อมกัน
3. **Frontend Map Integration** - แสดงเส้นทางบนแผนที่
4. **Distance Filter** - กรองหอพักตามระยะทางจากจุดที่กำหนด
5. **Walking/Driving Toggle** - ให้ผู้ใช้เลือกโหมดการเดินทาง

### Improvements:
- เพิ่มโหมด public transport
- รองรับหลายภาษา (EN/TH)
- แสดง step-by-step directions
- เพิ่มการคำนวณค่าใช้จ่ายเดินทาง

---

## 📝 สรุป

ระบบคำนวณระยะทางถูกสร้างสำเร็จแล้ว! ✅

**ความสามารถหลัก:**
- ✅ คำนวณระยะทางตามถนนจริง (OpenRouteService API)
- ✅ รองรับทั้งเดินเท้าและขับรถ
- ✅ Fallback system เมื่อ API ล้ม
- ✅ Chatbot ตอบคำถามระยะทางอัตโนมัติ
- ✅ API endpoints สำหรับ integration

**ขั้นตอนถัดไป:**
1. ทดสอบกับข้อมูลจริง
2. ตรวจสอบ performance
3. เพิ่ม caching ถ้าจำเป็น
4. รวมเข้ากับ Frontend UI

---

**สร้างเมื่อ:** 2025  
**เวอร์ชัน:** 1.0.0  
**สถานะ:** ✅ พร้อมใช้งาน (รอทดสอบ)
