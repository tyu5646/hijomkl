# ==================== SMART DORM ENVIRONMENT CONFIGURATION ====================
# คัดลอกไฟล์นี้เป็น .env และใส่ค่าจริงของคุณ
# 
# ⚠️  IMPORTANT: ไม่ควรใส่ข้อมูลจริงในไฟล์นี้!
# ⚠️  IMPORTANT: ไฟล์นี้จะถูก commit ขึ้น git, ไฟล์ .env จะไม่ถูก commit

# ==================== DATABASE CONFIGURATION ====================
# การตั้งค่าฐานข้อมูล MySQL
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_database_password_here
DB_DATABASE=smart_dorm
DB_PORT=3306

# ==================== JWT CONFIGURATION ====================
# คีย์สำหรับการเข้ารหัส JWT Token (ควรยาวอย่างน้อย 32 ตัวอักษร)
JWT_SECRET=your_super_long_and_complex_jwt_secret_key_here_minimum_32_characters

# ==================== SERVER CONFIGURATION ====================
# พอร์ตที่เซิร์ฟเวอร์จะรัน
PORT=3001
# สภาพแวดล้อม (development, production, test)
NODE_ENV=development

# ==================== AI/CHATBOT CONFIGURATION ====================
# API Key สำหรับ Groq AI (สำหรับ Chatbot)
GROQ_API_KEY=your_groq_api_key_here

# ==================== AWS S3 CONFIGURATION ====================
# การตั้งค่า AWS S3 สำหรับเก็บไฟล์ (หากใช้)
AWS_ACCESS_KEY_ID=your_aws_access_key_id
AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key
AWS_REGION=ap-southeast-1
AWS_BUCKET_NAME=your_s3_bucket_name

# ==================== EXTERNAL DATABASE (OPTIONAL) ====================
# สำหรับ PostgreSQL/Neon Database (หากใช้แทน MySQL)
# DATABASE_URL=postgresql://username:password@host:port/database?sslmode=require

# ==================== EMAIL CONFIGURATION (OPTIONAL) ====================
# การตั้งค่าอีเมล (หากมีฟีเจอร์ส่งอีเมล)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_email_app_password

# ==================== SECURITY SETTINGS ====================
# จำนวนรอบการเข้ารหัสรหัสผ่าน
BCRYPT_ROUNDS=12
# คีย์สำหรับ Session
SESSION_SECRET=your_session_secret_key_here
